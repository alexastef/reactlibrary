import React from "react";
import { HeroStyles } from "./Hero.styles";

const Hero = () => {
  return (
    <HeroStyles>
      <h1>REACT LIBRARY</h1>
      <h4>Search for and save all the books you want to read</h4>
    </HeroStyles>
  )
}

export default Hero;