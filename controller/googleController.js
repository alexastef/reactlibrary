const axios = require("axios");
const db = require("../models");

module.exports = {
  findAll: (req, res) => {
    console.log(req);
    // axios.get("https://www.googleapis.com/books/v1/volumes", {})
  }
}